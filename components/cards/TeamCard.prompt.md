Team member tile for navy sections — rounded photo, white name, coral "View bio" link that opens a modal.

```jsx
<TeamCard image="assets/people/rodger_kennedy.png" name="Rodger Kennedy" linkLabel="Rodger's story" onClick={openModal} />
```

Use in a 6-column grid (25px gap) inside a 2px-coral-bordered box; `size="lg"` for the founder spotlight.
