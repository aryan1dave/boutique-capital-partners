The site's sticky white header — BCP logo left, navy 18px Montserrat menu right, white dropdown with teal hover.

```jsx
<NavBar />
<NavBar items={[{ label: "Our services", href: "#services" }, { label: "Contact", href: "#contact" }]} />
```

Adjust `logoSrc` relative path to where your page lives (default expects project root).
