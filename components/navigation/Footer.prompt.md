Navy site footer with social circles, legal links, FCA compliance text and the cream logo + strapline.

```jsx
<Footer />
```

The compliance copy is mandatory furniture — keep it verbatim unless the user supplies new legal text. Adjust `logoSrc` for nested pages.
